<h2><%= t(:editing, :default => 'Editing') %> <%= t('app.models.product.bs_caption', :default => 'product') %></h2>

<%= render 'form' %>

<%= link_to t(:back, :default => "Back"), products_path, :class => "btn btn-default" %>